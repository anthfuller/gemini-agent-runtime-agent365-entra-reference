# Infrastructure as Code

This directory is reserved for future infrastructure as code.

No Terraform, Bicep, Microsoft Graph provisioning, or CI/CD deployment automation is included in the validated public baseline. Each future automation path requires its own least-privilege design, current API verification, review, and validation evidence.
